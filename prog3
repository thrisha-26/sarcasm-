github : create new repo(test) copy the link 
vs code :write simple java(myfile.java) code put it inside github repo file ro
          git bash: cd "path"
          git clone " http:/....../test"
          cd test
          git config --global user.email
          git config --global user.name
          git add myfile.java
          git commit -m "done"
          git push
jenkins:new item :program
        choose frestyle ->ok
        souce code manager :git ->paste url from git
                            master change to main
                            choose hook(optional)
                            build steps (execute windowscode
                           (javac myfile.java)
                            save
        build now : green marks
